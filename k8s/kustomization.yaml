apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm-gateway

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: llm-gateway
  app.kubernetes.io/component: api

images:
  - name: ghcr.io/your-org/llm-gateway
    newTag: latest
